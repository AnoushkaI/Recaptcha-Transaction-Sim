# fraud-rule-engine — Python backend dependencies
# MY SCOPE only (backend/ai/, backend/api/)
# Do NOT add teammate's core/ deps here without coordination

# ── Web framework (already installed globally, listed for completeness) ──
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
python-dotenv>=1.0.0

# ── AI orchestration ──
langgraph>=0.2.0
langchain-core>=0.3.0

# ── Gemini API (google-genai already installed, pinned for safety) ──
google-genai>=2.10.0

# ── HTTP / retry ──
httpx>=0.27.0
tenacity>=9.0.0

# ── Pydantic (shared data contracts) ──
pydantic>=2.0.0
